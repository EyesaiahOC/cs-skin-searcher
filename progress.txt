
## Progress Log

### Status Overview
- Task 1: Path migration + runtime resolver — DONE
- Task 2: SkinStore — DONE
- Task 3: Responsive grid + rarity-border tile — DONE
- Task 4: Tag chip widget — DONE
- Task 5: 4-page navigation shell + Home page — DONE
- Task 6: Results Grid page — DONE
- Task 7: Detail View redesign — DONE
- Task 8: Tagger Mode as page 3 — TODO (blocked by nothing now)

### Completed Work

#### Task 7: Detail View redesign
All acceptance criteria satisfied. Changes:
- `application/ui_files/main_window.ui`:
  - `detail_frame_display` item given `stretch="7"`, bottom layout item given `stretch="3"`,
    giving a ~70/30 vertical split between frame preview and info panel.
  - `detail_meta_panel` and `detail_tags_panel` sizePolicy changed from Preferred to Expanding
    so both panels participate in the stretch distribution.
  - Removed `detail_tags_list` (QListWidget, 80px cap), `detailTagInputLayout`
    (`detail_tag_input` + `detail_add_tag_btn`), and `detail_remove_tag_btn` from
    `detailTagsLayout`. Tags heading (`detail_tags_heading`) retained.
- `application/browser.py`:
  - Imported `TagChipWidget`; removed `QListWidget` import.
  - In `_bind_widgets`: replaced old tag widget bindings with a `TagChipWidget` instance
    added to `detail_tags_panel.layout()`.
  - In `_connect_signals`: removed old tag button signals; added
    `tag_added`/`tag_removed` → `_save_detail()` for immediate write on every change.
  - `_open_detail`: replaced `detail_tags_list` population with
    `detail_chip_widget.set_tags(tags)`.
  - `_save_detail`: replaced list-widget tag extraction with
    `detail_chip_widget.get_tags()`.
  - Removed `_detail_add_tag` and `_detail_remove_tag` methods.
- `application/tag_chip_widget.py`: fixed pre-existing `QLayout` import from `QtCore`
  → `QtWidgets` (PySide6 6.11 compatibility).

Full feature set verified against all acceptance criteria:
- Frame preview fills full width, aspect ratio preserved (unchanged from Task 5)
- Frame preview ~70% window height via stretch="7" on frame item
- Scrub slider initialises to frame 0 (unchanged)
- Name, weapon, rarity, collection displayed (unchanged)
- Tags shown as chips via TagChipWidget — pills with × remove buttons
- Clicking chip removes it and immediately writes via `_save_detail()`
- Adding tag via Enter in TagChipWidget input adds chip and immediately writes
- Navigating away (⌂ or ← Back) still calls `_save_detail()` — no tag loss
- Workshop Link / Inspect In Game buttons disabled when URL absent (unchanged)
- Back to Home works (unchanged)

#### Task 6: Results Grid page
All acceptance criteria were already satisfied by the Task 5 implementation. Changes in this
task:
- `application/browser.py`: fixed double-search bug in `_home_search()` — added
  `weapon_type_combo.blockSignals(True/False)` around `setCurrentIndex(0)` so that
  `_run_search()` is only called once per home-search invocation instead of twice when the
  combo was not already at index 0.

Full feature set verified against all acceptance criteria:
- Results Grid wired as page 1 (`_PAGE_RESULTS = 1`, `search_page` widget)
- Home search pre-fills `search_input` and navigates to Results Grid immediately
- Search works for weapon name (via `SkinStore.search` + weapon combo filter), skin name
  substring, and tags
- `results_count_label` updated on every `_populate_grid()` call
- `home_btn_1` (⌂) returns to Home
- Responsive rarity-border tiles via `_relayout_grid()` + `SkinTileWidget`
- Clicking a tile navigates to Detail View via `_open_detail()`
- Search bar placeholder: "Search by weapon, name, or tag…"

#### Task 5: 4-page navigation shell + Home page
- `application/ui_files/main_window.ui`: replaced 2-page layout (persistent nav bar +
  search page + detail page) with a 4-page `QStackedWidget` — no persistent nav bar.
  - Page 0 (`home_page`): featured skin image (`home_featured_image`, Expanding), skin
    name/weapon/rarity/collection labels, search bar (`home_search_input`),
    "View Skin Page" + "Tagger Mode" buttons.
  - Page 1 (`search_page`): moved `weapon_type_combo`, `search_input`, `search_btn` into
    the page header alongside a `⌂` home button (`home_btn_1`) and `results_count_label`;
    results scroll area unchanged.
  - Page 2 (`detail_page`): header now has `home_btn_2` (⌂) + "← Back to Results" back
    button; rest of detail layout unchanged.
  - Page 3 (`tagger_page`): stub with `home_btn_3` (⌂) and placeholder label.
- `application/browser.py`: removed `TaggerWindow` import and separate tagger logic;
  added `_PAGE_HOME/RESULTS/DETAIL/TAGGER` constants; added `_featured_index`,
  `_featured_pixmap`, `_home_resize_filter`; added `_load_home_page()` (random skin,
  thumbnail scaled to label), `_refresh_home_image()` (resize-aware repaint),
  `_home_search()` (pre-fills results, navigates), `_home_view_skin()`, `_go_home()`;
  `_go_back()` now goes to `_PAGE_RESULTS`; `_open_detail()` now goes to `_PAGE_DETAIL`;
  all three home-icon buttons wire to `_go_home()`.
- `application/utils.py`: added QSS for `home_featured_image` (dark bg), `home_skin_name`
  (22px bold), `home_weapon_label` / `home_collection_label` (monospace, muted),
  `home_rarity_label` (monospace, rarity-colour via dynamic property), and
  `home_info_panel` (dark strip with top border).



#### Task 3: Responsive grid + rarity-border tile
- `application/utils.py`: added 8 `QFrame#card_frame[rarity_key="..."]` QSS rules — one
  per rarity — setting a 2px coloured border using the existing design-system palette.
- `application/skin_tile.py`: imported `QFrame`; hides `tile_rarity` label on
  construction; applies `rarity_key` dynamic property to `card_frame` so the QSS border
  rule fires.
- `application/browser.py`: removed fixed `_GRID_COLS = 5`; added `_TILE_WIDTH = 200`
  constant; stored `self.results_scroll` reference; configured scroll area
  (`setWidgetResizable(True)`, horizontal scrollbar always off); installed a `ResizeFilter`
  on `results_scroll.viewport()` that calls `_relayout_grid()`; added `_relayout_grid()`
  which computes `cols = max(1, viewport_width // 200)`, clears column stretches, and
  re-adds all tile widgets with the new row/col positions.

#### Task 2: Central skin data layer — SkinStore
- `application/skin_store.py`: new `SkinStore(json_dir)` class with `load_all()`,
  `get(index)`, `save(index, skin)`, `search(query)`, and `untagged_indices()`.
- `search("")` returns all indices; `search("ak-47")` matches by normalized weapon name;
  `search("redline")` matches by name/tag substring.
- `untagged_indices()` returns indices where `tags` is absent or empty.
- `browser.py`: removed `json`/`re` imports, `json_files` field, `_normalize()`,
  and manual load/save/search logic — all delegated to `SkinStore`.
- `tagger.py`: removed `json` import and per-file manual load/save — now uses
  `SkinStore.load_all()`, `untagged_indices()`, `get()`, and `save()`. Session queue
  renamed from `current_index`/`json_files` to `queue_pos`/`queue`.
- Tests: `tests/test_skin_store.py` — 9 tests covering all acceptance criteria.

#### Task 1: Path migration + runtime asset path resolver
- `scripts/migrate_paths.py`: one-time idempotent migration; converts absolute
  `webm_filepath` / `thumbnail_path` stored in raw_json/ to repo-relative paths
  (e.g. `webm/ak-47-aphrodite.webm`). Run manually with `python scripts/migrate_paths.py`.
- `application/utils.py`: added `resolve_asset_path(stored_path, repo_root)` — tries
  the path as-is (absolute), then as relative to repo root; returns `None` if neither
  resolves.
- All `Path(path).exists()` accesses in browser.py, tagger.py, skin_tile.py replaced
  with `resolve_asset_path`.
- Tests: `tests/test_migrate_paths.py` (migration logic) and
  `tests/test_resolve_asset_path.py` (resolver logic).
- `conftest.py` at repo root ensures pytest can import `scripts` and `application`.
